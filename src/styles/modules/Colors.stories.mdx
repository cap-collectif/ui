import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs';
import colors from './colors';

<Meta title="Colors" />

# Colors

<ColorPalette>
  {Object.entries(colors).map(([colorName, colors], idx) => typeof colors === "string" ? (
    <ColorItem title={colorName} subtitle="" colors={[colors]} key={idx} />
  ) : (
    <ColorItem title={colorName} subtitle="" colors={colors} key={idx} />
  ))}
</ColorPalette>